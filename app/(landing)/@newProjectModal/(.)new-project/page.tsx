import { NewProjectModalContent, Modal } from "@/app/ui";
import React from "react";

const Page: React.FC = () => {
  return (
    <Modal>
      <NewProjectModalContent />
    </Modal>
  );
};

export default Page;
